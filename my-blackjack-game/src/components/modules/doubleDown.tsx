import React from "react";

const DoubleButton = (props:{gameState:string, handID:number, playerID:string})=>{
    return(
           <button className='game-btn double-btn'>Hit</button>
    )
}

export default DoubleButton;